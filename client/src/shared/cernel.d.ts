declare type ID = string | number;

declare type Point = { x: number; y: number };
declare type Vector = { i: number; j: number };
declare type Sizes = { width: number; height: number };
declare type Opt<T> = T | undefined | null;
